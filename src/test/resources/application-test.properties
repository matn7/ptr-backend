spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.datasource.initialization-mode=always
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql = true

## sql scripts
## -- Disable FK check for H2 in memory DB - for test purposes only
sql.script.disable.fk.check=SET REFERENTIAL_INTEGRITY FALSE;
## --insert plan
sql.script.create.plan=insert into plan(id, name) values(1,'Basic');

## --insert user
sql.script.create.userEntity=insert into user_entity(id,username,password,email,first_name,last_name,enabled,plan_id) values(30,'matek_1991','M@jkaM@jka1!','mateusz.nowak@pandatronik.com','majka','majka',1,1);
## --delete user
sql.script.delete.userEntity=delete from user_entity;

## --insert days
sql.script.create.days=insert into days(id,body,rate_day,posted_on,start_date,user_id_id) values(300,'Days Entry',1,'2024-03-24 18:27:41.000000','2024-03-01',30)
## --delete days
sql.script.delete.days=delete from days;
sql.script.truncate.days=truncate table days restart identity;
sql.script.restart.sequence.days=alter sequence days_seq restart with 1;
sql.script.check.sequences.days=SELECT SEQUENCE_NAME FROM days.SEQUENCES;
